{
  "name": "demo-app-notification",
  "version": "0.1.1",
  "description": "Demonstrates sending a notification and setting a badge count",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && vite build"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "vite": "^4.3.3"
  },
  "dependencies": {
    "@meetportal/portal-js": "^0.4.6"
  }
}
